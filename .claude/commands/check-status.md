Current time: !`date`
Branch: !`git branch --show-current`
Uncommitted changes: !`git status --short`

Check current project status:
1. List all modules in `api/modules/` and their status
2. Check if docker-compose services are running
3. Show recent git log (last 5 commits)
4. Check for any TODO/FIXME comments in changed files
5. Report: what's done, what's in progress, what's blocked
