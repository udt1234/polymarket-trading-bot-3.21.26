# Re-audit #4: pattern_discovery_2026-07-26 (post-cadence pattern discovery, scope class b)

**Auditor:** `@backtest-auditor` (fifth pass, post-repair). **Date:** 2026-07-31.
**Verdict history:** FAIL -> WARN -> WARN -> PASS -> **WARN** (this round).
**Scope:** (b) pure forecast-accuracy / calibration diagnostic. No P&L, no fills. Pass B fill/fee battery does not apply.
**Pre-registration:** `_DataMetricPulls/pacing_backtest/prereg/pattern_discovery_2026-07-26.md`
**Trigger for this round:** two independent data-layer repairs since the 2026-07-29 PASS: (1) canonical/prices winner-coverage rebuild (2026-07-30 12:36, external), (2) five-site DST/day-boundary fix found while auditing the sibling regime study.

## VERDICT: WARN

No fatal finding. Reproduction is exact, the coverage repair is independently verified true at the ground-truth canonical-parquet level (not just trusted from the script own report), the DST fix is complete and correct at all 5 declared sites with no 6th found, and every previously-cleared gate (b4_admissible, SUCCESS_N_FLOOR, RNG split, trial_count=125, THE WALL, noon-ET parsing, model_version) still holds under fresh re-verification. **The headline itself is TRUE and reproduces exactly: no method beats the market on the full repaired sample (n=27/20/19).** This is downgraded from the prior PASS to WARN because the deliverable itself (not the underlying data or numbers) contains a confirmed stale/self-contradictory claim and an undisclosed change campaign -- the same class of defect (verify-do-not-infer / false-disclosure) that earned re-audit #2 a WARN even though its numbers also reproduced exactly. Both issues are fixable by editing prose, not by touching the model or the data.

## Reproduced headline number: YES (exact, independently rebuilt from per_row.csv, not copied from the runner)

| target | n_full | n_b4_admissible | PRIMARY beats-B4 (mine) | PRIMARY beats-B4 (reported) |
|---|---|---|---|---|
| elon_2day sealed | 28 | 27 | none (all methods LOSE or unproven vs B4) | none |
| trump_7day sealed | 20 | 20 | none (all methods LOSE or unproven vs B4) | none |
| elon_7day sealed | 19 | 19 | none (all methods LOSE or unproven vs B4) | none |

B1 vs B4 / B3 vs B4 skill, independently recomputed vs summary.md, matched to 4 decimals on every target:
- elon_2day sealed: B1 vs B4 = -0.3355 [-0.6841,-0.0596] n=27 (mine) vs -0.3355 (reported); B3 vs B4 = -0.2196 vs -0.2196.
- trump_7day sealed: B1 vs B4 = -0.7279 [-1.3216,-0.2066] n=20 vs -0.7279; B3 vs B4 = -0.4144 vs -0.4144.
- elon_7day sealed: B1 vs B4 = -0.0205 [-0.2789,+0.2208] n=19 vs -0.0205; B3 vs B4 = -0.0881 vs -0.0881.

`b4_admissible` column independently re-derived from B4 own winner-row `market_price` and diffed against the persisted column: 0 mismatches across all 225,039 rows.

Also independently re-derived coverage from the canonical/prices parquet directly (bypassing the script own `check_token_price_coverage`, using only the unchanged `pattern_discovery_lib.py` loaders): elon_2day n=66/0 missing, elon_7day n=78/0 missing, trump_7day n=42/0 missing -- confirms the 0.0% figures at the ground-truth data level, not merely at the script self-report level.

## Findings (most severe first)

### [CLASS C] FINDING 1 (this round, MEDIUM-HIGH) -- stale, self-contradictory coverage-gap paragraph survives in summary.md, describing a bug as current that this run data proves is fixed
Where: `pattern_discovery_out/summary.md:11-15`. Lines 11-13 report, correctly, "winner bracket missing from canonical/prices for 0 auctions (0.0%)" for all three targets. Line 15, two lines later in the same section, reads: "Elon coverage is far worse than the historically-quoted ~27% ... Measured directly here on the full canonical/prices table: the gap is Elon-specific (Trump = 0% missing) and does not correlate with the winner being an extreme/thin bracket ... likely `03_build_auctions.py` re-running after `08_normalize_bucket_labels.py` demotion pass silently reverted the demotions."

This is leftover text from before the 2026-07-30 coverage repair. It describes an ongoing gap (present tense, "the gap IS Elon-specific") and a live root-cause hypothesis, directly underneath two lines stating the current gap is 0.0% for both Elon targets. A reader working through this section top-to-bottom hits a flat contradiction, with no percentage given for "far worse than ~27%" and no acknowledgment that the described bug has since been fixed. Independently confirmed the 0.0% figures are real (see ground-truth re-derivation above), so line 15 is the stale element, not lines 11-13.

Why this matters: this project has been burned by exactly this failure mode before (re-audit #2 confirmed-false "unchanged" claim, WARN-severity). A reader who trusts line 15 over 11-13 (a reasonable reading, since it presents as the authoritative explanation) would wrongly discount this run B4 comparisons as resting on a two-thirds-missing-data foundation, when the true admissibility loss is now only 70/781 (9.0%) checkpoint-level rows (per the still-accurate Finding-1-B4-admissibility section directly below it at summary.md:17-19), not 63.6%/78.2% of auctions.

Fix: delete or rewrite summary.md:15 to state the coverage gap in the past tense as a fixed, historical issue (e.g. "A 2026-07-30 canonical/prices rebuild fixed a winner-token coverage gap that previously affected 63.6%/78.2% of elon_2day/elon_7day auctions [root cause: `03_build_auctions.py` re-running after `08_normalize_bucket_labels.py` demotion pass silently reverted the demotions]; current coverage is 0.0% missing on all three targets, independently re-verified against canonical/prices directly for this audit.").

### [CLASS C] FINDING 2 (this round, MEDIUM) -- the entire 5-site DST/day-boundary fix campaign is undisclosed anywhere in the deliverable (RUN_META.notes and summary.md), breaking the established self-documentation convention of this exact study
Every prior repair round in this study got an explicit, dated, named section (the B4 admissibility fix section, the RNG-split note, the SUCCESS_N_FLOOR reporting-floor note, the reaudit2 false-claim correction). This round DST fix -- five sites (DailySeries, r2_null_check, M7 daily decomposition, the Trump day-zero anchor, pattern_discovery_methods.py:282 M3 day-end) -- has zero corresponding section. RUN_META.notes still ends at the reaudit2_2026-07-29_fixes entry; grepped for "2026-07-31" / "dst" / "coverage_repair" in the notes string: none found. summary.md contains exactly one "2026-07-31" mention (the new, unrelated activity-shift disclosure header) and zero mentions of DST, add_days_et, or any day-boundary language.

Confirmed real and material, not cosmetic: independently diffing the COVERAGEFIX per_row.csv against the DSTFIX1 per_row.csv (isolating the DST-fix-only delta) shows 32,132 model_prob rows changed on the three auction-scored targets (M2: 5,017 rows up to 0.39 abs diff; M5: 2,296 rows up to 0.59; M6: 8,076 rows up to 0.14; M7(M1): 7,791 rows up to 0.07; M7(M6): 8,952 rows up to 0.17), plus 2,671 M3 rows on elon_daily from the DSTFIX1-to-PASS1 diff (isolating the standalone M3 fix). None of this -- which methods moved, by how much, or why -- is recorded anywhere in the artifact a downstream reader would consult.

Structural explanation for WHY B1/B2/B3/B4/M1/M4 are immune while M2/M5/M6/M7/M3 move (requested check, confirmed clean): run_auction_target (pattern_discovery_2026-07-26.py:404) builds trailing_fn = lambda T: daily_series.trailing_mean(T) from the same DailySeries object the DST fix corrects, and both select_M2/fit_M2/build_features (M2) and select_M5/fit_M5/predict_M5 (M5) consume trailing_fn/prior_final_fn as declared prereg features ("prior window final count; trailing 7-day mean daily count"). B1/B2/B3/M1/M4/B4 do not consume these DailySeries-derived features (B1 uses o/eh/rh plus its own error history; M1 is an empirical table on c,o; M4 is an hourly-count HMM; B3 is locked_pace; B4 is the market price). M6/M7 move because they are themselves built directly from DailySeries/add_days_et day-boundaries. This is a clean, verified structural explanation -- not itself a finding -- but it is exactly the kind of explanation that belongs in the deliverable own disclosure text and currently is not there.

Also confirmed, not merely inferred: the informal characterization relayed alongside this audit brief ("every DETERMINISTIC method bit-identical ... Only M6 (stochastic) and M7 (daily-decomposing) moved, by 0.003 to 0.17") is materially incomplete when checked against the data -- M2 (a deterministic CART, not M6/M7) and M5 also moved, and the true magnitude range is up to 0.76 on the train span / 0.59 on sealed, not 0.17. This does not change any conclusion (none of M2/M5/M6/M7 ever produces a beats-B4 result in either the pre- or post-DST-fix run) and was not itself baked into the audited artifact, so it is not a second confirmed-false-claim-in-the-deliverable finding -- but it reinforces Finding 2: an undocumented change invites exactly this kind of informal mischaracterization, and the fix is the same either way (write down, once, in the artifact, precisely what changed).

Fix: add a DST / day-boundary fix section to summary.md and a corresponding dst_fix_2026-07-31 entry to RUN_META.notes, naming the 5 sites, the 126-day drift window (2025-11-02 to 2026-03-07, confirmed below), and the affected-methods/magnitude table above.

### [CLASS D] Confirmed resolved -- all 5 declared DST fixes are real, complete, and correctly implemented; no 6th DST-anchoring bug found
Independently re-derived the drift window rather than trusting the description: diffed auction_slug unix timestamps for the elon_daily target between the COVERAGEFIX_preDST snapshot (pre-DST-fix) and the DSTFIX1 snapshot (post-fix). Exactly 126 of 285 daily anchors differ, all by exactly plus-3600s, spanning 2025-11-02T16:00:00Z (old) to 2025-11-02T17:00:00Z (new) through 2026-03-07, i.e. precisely the US DST-standard-time window (fall-back 2025-11-02 to spring-forward 2026-03-08) -- an exact, independently-reproduced match to the claimed repair, not a trusted description.

Code review, all 3 files:
- add_days_et() (pattern_discovery_2026-07-26.py:101-105) uses pd.Timestamp(...).tz_convert(ET) plus pd.DateOffset(days=n) -- calendar-aware, wall-clock-preserving. Independently tested in isolation against both 2025-2026 US transition dates: Timestamp(2025-11-01 12:00, ET) plus DateOffset(days=1) correctly lands on 2025-11-02 12:00:00-05:00 (not 11:00 or 13:00), and Timestamp(2026-03-07 12:00, ET) plus DateOffset(days=1) correctly lands on 2026-03-08 12:00:00-04:00, with the underlying elapsed seconds correctly showing 25h (fall-back day) and 23h (spring-forward day) respectively.
This is the class of fix the sibling regime study needed and did not have (Timestamp(date,tz) plus Timedelta(hours=12), absolute-duration arithmetic that misfires ON the transition date itself). This codebase fix does not share that residual flaw -- confirmed by direct construction and comparison, not by re-reading the comment.
- DailySeries.__init__ (:118-119), M7 daily-decomposition day_idx/day_start/day_end/next_day_end (:492-516), build_targets Trump day-zero anchor (:580-583, defensive -- constructed directly via pd.Timestamp(year=,month=,day=,hour=12,tz=ET), never accumulated, so it was never exposed to the bug class), and r2_null_check (:856-876) all call add_days_et() for every day-boundary walk.
Grepped all three files for 86400 / Timedelta(days / Timedelta(hours: the only two remaining 86400 hits are (a) comments describing the fix and (b) pattern_discovery_methods.py:493 _circadian_mult 120-day lookback filter, which is a coarse rolling-window cutoff for the M6 hourly-multiplier histogram, not a day-boundary anchor used for resolution, labeling, or THE WALL -- a plus-or-minus 1h fuzziness at the 120-day-ago edge of a lookback filter is immaterial and does not misalign any auction window, decision checkpoint, or causal boundary.
pattern_discovery_lib.py (noon-ET slug parsing) has zero 86400/Timedelta hits and was not touched (mtime unchanged since 2026-07-26 12:04:29) -- correct, since noon_window() constructs each auction start/end directly per-auction rather than walking forward from an anchor, so it was never exposed to this bug class.
- pattern_discovery_methods.py:282-286 (M3 within-day remaining-count feature) now reads d["e"] (the DailySeries-defined, DST-aware day end) instead of d["s"] + 86400. Independently confirmed this is the only change between the DSTFIX1 and PASS1 runs (2,671 rows, 100% M3/elon_daily, 0 rows changed on any other target or method) -- the fix is surgical, touches exactly what it claims, and nothing else.
- WALL_ELON/WALL_TRUMP/elon_clean_start/elon_clean_end (:47-48,564-565) are all direct single-instant pd.Timestamp("...", tz=ET) constructions, not accumulated walks -- not exposed to this bug class, unchanged, and re-verified equal to the pre-registration declared dates.
- Elapsed-hour checkpoint offsets (T = u["s"] + c*3600, used throughout for the prereg declared "hours into the noon-ET window" checkpoints) are intentionally fixed-duration elapsed-time additions, not calendar-day anchors -- correctly out of scope for this bug class, confirmed by the r2_null_check own inline comment making this distinction explicit.

### [CLASS A/D] Confirmed resolved -- coverage repair is genuine at the ground-truth data layer, isolated correctly, and does not leak into any non-B4 method
Full-column diff, STALE per_row.csv vs COVERAGEFIX_preDST per_row.csv: model_prob differs on exactly 10,583 rows, 100% method==B4 (the only method whose model_prob IS the market price). market_price/price_coverage_n/price_coverage_tot/b4_admissible all update consistently across every method-row sharing that (auction, checkpoint, bracket) key (informational columns, expected). is_winner (the ground-truth label) is 0/225,039 different -- confirms no leaked-label or selection-on-outcome contamination rode in with the repair. No B1/B2/B3/M1/M2/M4/M5/M6/M7 forecast values changed due to the coverage repair (expected -- none of them read market prices as a feature).
Coverage re-derived from scratch against the live canonical/prices parquet files directly, using only the unchanged, already-audited-clean pattern_discovery_lib.py loaders and completely bypassing the pattern_discovery script own check_token_price_coverage function: elon_2day 66/0 missing (0.0%), elon_7day 78/0 missing (0.0%), trump_7day 42/0 missing (0.0%) -- exact match to the reported figures, confirmed at the data layer, not merely the script self-report.

### [CLASS D] Confirmed resolved -- determinism holds under independent byte-comparison
md5sum of the PASS1 per_row.csv and the live per_row.csv: both 1d8b526cfe600654c37c57e65b6cde84, byte-identical, 225,039 data rows each. The DSTFIX1 per_row.csv (pre-M3-fix) has a different MD5 (ded1b50f...), as expected, isolating the M3 fix effect precisely. Two independently-triggered cold runs from cleared checkpoints reproduce identically; the seeded RNG design (RNG_SEED=20260726, independent RNG_M5/RNG_M6 offsets) holds.

### [CLASS D] Confirmed resolved -- elon_7day n=19 result is a genuine, ungated test, not a masked or defaulted artifact
Independently recomputed all 9 method-vs-B4 skill/CI/jackknife rows for elon_7day sealed (n=19) directly from per_row.csv (table above). Every method 95% block-bootstrap CI either straddles zero (unproven) or sits entirely below zero (loses); none has ci_lo greater than 0. SUCCESS_N_FLOOR=10 no longer applies (19 is greater than or equal to 10), so the "none" flag is a real, ungated verdict, not the n=3 noise-sentinel this same comparison carried in the pre-coverage-repair (STALE) run. Confirms the coverage repair did not silently flip this target conclusion by relaxing an existing safety gate -- it genuinely changed the sample from statistically dead (n=3) to a full, real test that still resolves to "no edge."

### [CLASS D] Confirmed resolved -- r2_null_check correction is real, correctly attributed to the DST fix alone, and changes no conclusion
STALE run null_check: hour18=0.7641213008523999, hour21=0.8560668462342742 (the "0.764/0.856" figures quoted all session). COVERAGEFIX_preDST run: identical to STALE (confirms the coverage repair did NOT touch this figure). DSTFIX1/PASS1/live runs: hour18=0.7444265811605689, hour21=0.8698967594077209 (matches summary.md reported "hour18=0.744, hour21=0.870" exactly). Grepped r2_null_check only two consumers (write_summary display string, gated on a greater-than-0.5 threshold both before and after the fix; and the RUN_META.notes blob): purely descriptive, feeds no model, gate, bin edge, or hyperparameter selection anywhere in the file. No conclusion drawn against the old 0.764/0.856 changes as a result of the corrected 0.744/0.870.

### [CLASS D] Confirmed resolved -- activity-shift disclosure is accurate, freshly computed this run, and feeds nothing downstream
compute_elon_activity_shift() (pattern_discovery_2026-07-26.py:908-929) is computed independently from this script own daily_elon/WALL_ELON (not copied from the sibling regime study, per its own docstring). Verified: mean 44.2 (train, n=212 raw daily-series days) vs 30.7 (sealed, n=87 days) -- matches summary.md exactly. Grepped every use of its return value: appears only inside write_summary display-text lines 942-949, never read by any model-fitting, bin-edge, hyperparameter-selection, or gating code path. The n=212 (raw daily count, unfiltered) vs n=198 (elon_daily own scored train count, headline section) discrepancy is explained cleanly by run_daily_target if-len(priors)-less-than-10-continue warm-up filter (:646) dropping the first ~14 train days before enough prior history exists to score them -- not an inconsistency.

## What was checked and passed (fresh this round, adversarial, not carried over on trust)

- Prereg integrity: prereg/pattern_discovery_2026-07-26.md mtime 2026-07-26 11:21:06.956665, bit-identical to all four prior audit rounds. Untracked in git (no repo history exists for it), mtime is the strongest available signal and shows zero edits across the entire five-round campaign.
- Full-column diffs across all 4 intermediate output snapshots (STALE, COVERAGEFIX_preDST, DSTFIX1, PASS1, live) confirm the changes are isolated exactly to the two declared repairs, with no third, undisclosed category of row-level change found anywhere in the 225,039-row ledger.
- SUCCESS_N_FLOOR=10 (:65), _headline_flag gating logic (:889-905), the b4_admissible post-hoc column derivation (:1310-1315), RNG_M5/RNG_M6 independent seeding (pattern_discovery_methods.py:36-38), trial_count arithmetic (32+38+4+12+8+4+24+3=125), THE WALL, and model_version (matches api/modules/shared/locked_pace.py:22 exactly) -- all re-grepped against the live 2026-07-31 script and confirmed intact.
- Reproduction: all three primary_success_beats_B4 flags plus every headline skill/CI/n value for elon_2day, trump_7day, elon_7day sealed independently rebuilt from per_row.csv alone, in a standalone script, matching summary.md to 4 decimals.
- Coverage repair verified at the ground-truth canonical/prices parquet layer directly, bypassing the script own self-report entirely.
- DST fix verified complete (5 of 5 sites), correct (independently tested DateOffset(days=n) against both 2025-2026 US transition dates), and non-leaking outside its declared scope.
- Determinism: byte-identical MD5 across two independently-triggered cold runs.

## What could NOT be checked

- Did not re-derive the root cause of the original 63.6%/78.2% coverage gap at the canonical-builder level (03_build_auctions.py / 08_normalize_bucket_labels.py interaction) -- this was external repair work, out of scope for this backtest own code; only the downstream re-measurement was verified (and independently confirmed true).
- Did not obtain a true source-control diff of the script across rounds (files remain untracked in git); relied on full-column data diffs across the five preserved output snapshots instead, which is strong evidence for what changed in the computed output but not a line-by-line guarantee against a dead or unreached code path also being edited.
- Did not re-run the full pipeline end-to-end from raw parquet myself (reproduction was done from the emitted per_row.csv ledger per the audit protocol first-class path); relied on the preserved intermediate snapshot directories for the isolation diffs, which were not independently re-generated by this audit.

## What this WARN means and does not mean

It does NOT mean the headline is wrong. Every reported number -- coverage (0.0% missing, all three targets, verified at the raw-parquet layer), the DST fix (5 of 5 sites complete and correct, no 6th found, no residual transition-date flaw), determinism (byte-identical), and the headline itself (no method beats the market on the full repaired sample, n=27/20/19 sealed) -- independently reproduces exactly. This is a stronger, not weaker, result than the prior PASS: the market-efficiency finding now rests on 27/20/19 real auctions instead of 19/20/3, with elon_7day previously statistically dead and now a full n=19 test that still resolves to "no edge."

It means the deliverable is not yet internally honest enough to stand alone. Two prose-only, non-fatal defects survive: a stale paragraph in summary.md that flatly contradicts the coverage numbers stated two lines above it, and a complete absence of disclosure for a fix campaign that moved 32,132 plus 2,671 rows across five methods. Both are fixable without touching a single model, gate, or data value -- the underlying science does not need to be redone, the writeup does.

What it still does not establish (carried forward, unchanged from the prior PASS own caveat): this is a market-efficiency finding on the auctions the market actually priced at the tested checkpoints, not proof the market is efficient everywhere. The one residual full-auction exclusion (1 elon_2day auction with zero price coverage at any tested checkpoint) remains untestable by this study own PRIMARY criterion by construction, not confirmed either way.
