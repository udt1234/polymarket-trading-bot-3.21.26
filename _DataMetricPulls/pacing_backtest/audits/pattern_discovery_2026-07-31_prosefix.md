# Re-audit #5: pattern_discovery_2026-07-26 (post-cadence pattern discovery, scope class b) -- prose-fix mechanism check

**Auditor:** `@backtest-auditor` (sixth pass, focused re-check of the two 2026-07-31 WARN findings + the fix mechanism).
**Date:** 2026-07-31.
**Verdict history:** FAIL -> WARN -> WARN -> PASS -> WARN (postrepair) -> **PASS** (this round).
**Scope:** (b) pure forecast-accuracy / calibration diagnostic. No P&L, no fills. Pass B fill/fee battery does not apply.
**Pre-registration:** `_DataMetricPulls/pacing_backtest/prereg/pattern_discovery_2026-07-26.md`
**Trigger for this round:** builder's fix for the two `postrepair` WARN findings, after the orchestrator caught and corrected the first fix attempt (hand-edited `summary.md`/`run_meta.json` -> moved into `write_summary()` and re-run).

This round does not re-derive the science (models, gates, coverage repair, DST fix correctness) -- that was fully cleared in the `postrepair` pass and nothing about it changed. This round verifies only: (1) the fix mechanism is sound (script-generated, survives a fresh run, no hand-edited state), and (2) the two findings' text is now accurate.

## VERDICT: PASS

Both `postrepair` findings are resolved, correctly and durably. The prose now lives inside `write_summary()`/`emit_run_meta(notes=...)` in `pattern_discovery_2026-07-26.py`, was regenerated by an actual script run (not hand-pasted), and the regenerated output is verified byte-for-byte against the pre-fix hand-edited backup with exactly one line of expected difference. No numeric value moved anywhere in the campaign. No fatal or medium finding remains.

## Reproduced headline number: YES (unchanged from `postrepair`; re-confirmed present in the live artifact)

`summary.md:355,362,369` -- all three targets print `PRIMARY (tradeable) beats B4: NONE (all methods lose to B4)` for elon_2day (n=27), trump_7day (n=20), elon_7day (n=19) sealed. `run_meta.json` headline block: `n_auctions_b4_admissible = {elon_2day:27, trump_7day:20, elon_7day:19}`, all three `primary_success_*_beats_B4` flags empty/none. Matches the `postrepair` audit's independently-rebuilt table exactly.

## Mechanism verification (the part that matters more than the prose)

**1. Both prose fixes are genuinely generated by `write_summary()`/`emit_run_meta()`, not pasted into the artifact.**
Read the live script directly:
- `pattern_discovery_2026-07-26.py:967-982` -- the rewritten coverage-history paragraph (Finding 1 fix) is a `lines.append(...)` call inside `write_summary()`, in past tense throughout the historical portion ("was missing", "was Elon-specific", "was NOT a reliable filter") and closing with a dynamically-labelled present-tense state ("Current state, this run: coverage is 0.0% missing on all three targets").
- `pattern_discovery_2026-07-26.py:1005-1060` -- the new `## DST window-drift fix (audit 2026-07-31)` section is 14 `lines.append(...)` calls inside `write_summary()`, including a dynamically-rendered R2 null-shift line (`hour18 0.764 -> {null_check['hour18']:.3f}, hour21 0.856 -> {null_check['hour21']:.3f}`) that reads live from the `null_check` dict computed earlier in `main()`, not a hardcoded string.
- `pattern_discovery_2026-07-26.py:1454-1479` -- a `dstfix_2026-07-31_fixes=[...]` entry inside the f-string passed to `emit_run_meta(notes=...)`, i.e. part of the RUN_META call itself, not a post-hoc sidecar edit.

**Decisive dynamic test, not just code reading:** re-ran the script (`run_writesumfix_2026-07-31.log`). All four targets loaded from checkpoint pickles with cache-hit counts, no refit triggered ("elon_daily: loaded 64125 rows ... (+31 trials)" etc.), total runtime 19.7s. Both new sections reappeared verbatim in the freshly-written `summary.md` and `run_meta.json`. This is the fresh-run survival test the orchestrator required, and it passed: the fix is not artifact-only.

**2. No hand-edited state survives.**
`pattern_discovery_out/_prewritesumfix_backup_2026-07-31/` preserves the state immediately before this fix. Diffed against the live directory:
- `per_row.csv`: MD5 1d8b526cfe600654c37c57e65b6cde84 on both sides -- byte-identical (expected: this fix touches only summary text, not model rows).
- `pattern_discovery_2026-07-26.run_meta.json`: diff exit code 0 -- byte-identical.
- `summary.md`: differs by exactly one line, 39c39 (see below).

No other file in the live `pattern_discovery_out/` root is a leftover from the earlier hand-edit; the live `summary.md`/`run_meta.json`/`per_row.csv` mtimes (2026-07-31 15:05) match the `run_writesumfix_2026-07-31.log` completion timestamp exactly, i.e. they are the direct product of this run, not a stale hand-patched file left in place.

**3. RUN_META notes are script-resident.** Confirmed above -- the `dstfix_2026-07-31_fixes=[...]` text lives inside the f-string literal passed to `emit_run_meta(notes=...)` at `pattern_discovery_2026-07-26.py:1454-1479`, appended to the existing chain of `audit_2026-07-29_fixes` / `reaudit_2026-07-29_fixes` / `reaudit2_2026-07-29_fixes` entries this study has used for every prior repair round. The live `run_meta.json` notes field contains this text verbatim.

## Builder's specific claims, checked one by one

- "Re-run loaded all four targets from existing checkpoints, zero refits, runtime 19.7s vs 1380s for the cold-fit run." CONFIRMED. `run_writesumfix_2026-07-31.log` shows all 4 checkpoint-load lines (cache hits) and `[total runtime] 19.7s`. The comparison cold-fit run, `run_pass2.log`, shows `[total runtime] 1380.2s` -- matches "1380s" to one decimal.
- "`per_row.csv` MD5 1d8b526c... identical before and after; all 225,039 rows unchanged." CONFIRMED. MD5 1d8b526cfe600654c37c57e65b6cde84 on both the live file and the `_prewritesumfix_backup_2026-07-31` copy.
- "`run_meta.json` diff exit 0, byte-identical to the prior staged version." CONFIRMED.
- "`summary.md` differs by exactly one line (39c39), inside the DST-fix paragraph, where a redundant hardcoded 0.744/0.870 literal was dropped because the f-string already renders those values dynamically one clause earlier. Line counts 493 = 493." CONFIRMED, checked carefully:

  39c39
  < ... (the "Null-model check" section above already reports the corrected figures ...)
  > ... (the "Null-model check" section above already reports the corrected 0.744/0.870 figures ...)

  `wc -l` on both files: 493 = 493. No information is lost -- the values 0.744/0.870 are rendered dynamically via `{null_check['hour18']:.3f}`/`{null_check['hour21']:.3f}` earlier in the same sentence (the "hour18 0.764 -> 0.744" clause) and again in the "Null-model check" section it cross-references (`pattern_discovery_2026-07-26.py:1001-1003`, also dynamically rendered from `null_check`). The dropped literal was a genuine duplicate, not a fact removed from the reader.
- "All three PRIMARY (tradeable) beats B4: NONE lines present." CONFIRMED at `summary.md:355,362,369`.

## Finding 1 (from postrepair) -- resolved

`summary.md` (source `pattern_discovery_2026-07-26.py:967-982`) now reads unambiguously: history in past tense ("when this study was first run, the winner-bracket price was missing for 63.6%/78.2%... far worse than the historically-quoted ~27% (that figure was measured on a smaller, 22-auction pmxt-covered subset)... likely `03_build_auctions.py` re-running after `08_normalize_bucket_labels.py`'s demotion pass silently reverted the demotions. That gap is why the earlier admissible-n figures behind this study's headline were elon_2day=19, trump_7day=20 (unaffected), elon_7day=3, rather than the current 27/20/19."), followed by an explicit fix event ("A canonical/prices rebuild on 2026-07-30 12:36 ... fixed the gap"), and closing with an unambiguous present-tense state claim ("Current state, this run: coverage is 0.0% missing on all three targets", cross-referencing the three lines directly above it and noting it was independently re-verified against the raw parquet, not merely the script's self-report). No remaining present-tense claim that coverage is currently bad. The history explaining why the earlier n's were 19/20/3 is preserved, as required.

## Finding 2 (from postrepair) -- resolved

`summary.md` now contains a `## DST window-drift fix (audit 2026-07-31)` section (script-generated, confirmed above) that states, checked line by line against the `postrepair` audit's own independently-derived numbers:
- All five sites, matching the `postrepair` audit's code-review list exactly: `DailySeries`, `r2_null_check`, `run_auction_target`'s M7 daily decomposition, `build_targets`'s Trump day-zero anchor (correctly labelled a defensive-only fix, never actually exposed to the bug class), and `pattern_discovery_methods.py:282` (M3).
- The 126-day drift window, 2025-11-02 to 2026-03-07 -- matches the `postrepair` audit's independently-reproduced figure exactly (126 of 285 daily anchors, all +3600s).
- Why train-fitted artefacts were contaminated though sealed windows self-corrected -- states the TRAIN span contains all 126 drifted days so bin edges/terciles/CV-selected hyperparameters/cluster centroids/HMM parameters were fit on misaligned windows, matching the mechanism the `postrepair` audit verified structurally (trailing_fn/prior_final_fn consumed by M2/M5; M6/M7/M3 built directly from DailySeries).
- Corrected magnitudes: "32,132 model_prob rows changed across M2 (5,017 rows, up to 0.39 abs diff), M5 (2,296 rows, up to 0.59), M6 (8,076 rows, up to 0.14), M7(M1) (7,791 rows, up to 0.07), M7(M6) (8,952 rows, up to 0.17)... plus 2,671 rows on elon_daily, 100% M3... True magnitude range up to 0.76 on TRAIN / 0.59 on SEALED... B1, B2, B3, M1 and M4 were unchanged (bit-identical)." Verified against the per-method breakdown in the `postrepair` audit log Finding 2: the five per-method row counts sum to 5,017+2,296+8,076+7,791+8,952 = 32,132 exactly; every number matches the `postrepair` audit's independently-diffed figures 1:1. The section also explicitly self-corrects the earlier informal "only M6/M7 moved, by 0.003-0.17" characterization the `postrepair` audit flagged as inaccurate, naming it as such.
- The R2 null shift 0.764/0.856 -> 0.744/0.870 as a direct consequence -- present, dynamically rendered (see mechanism section above), matches the `postrepair` audit's independently-recomputed hour18=0.7444265811605689, hour21=0.8698967594077209.
- No PRIMARY verdict changed -- explicitly stated ("All three remained 'none' before and after").
- The two-cold-run determinism check -- explicitly stated ("two independently-triggered cold runs post-fix reproduce a byte-identical per_row.csv (MD5 match), summary.md, and RUN_META").

`RUN_META.notes` carries the equivalent `dstfix_2026-07-31_fixes=[...]` entry (verified live in `run_meta.json`, matches `summary.md`'s section content). This resolves the postrepair Finding 2 requirement that both `summary.md` and `RUN_META.notes` carry the disclosure.

No inaccuracy found in the fix section against the ground-truth numbers already independently derived in the prior audit round -- unlike the informal characterization it explicitly corrects, this section's magnitudes check out exactly.

## What was checked and passed (this round)

- Mechanism: both fixes live inside `write_summary()`/`emit_run_meta(notes=...)` source, confirmed by direct code read at the cited line numbers.
- Dynamic confirmation, not just static code reading: re-ran the script; all 4 targets loaded from checkpoint (no refit), 19.7s runtime; regenerated `summary.md`/`run_meta.json`/`per_row.csv` are the artifacts audited, timestamped to the run.
- `per_row.csv` MD5 identical pre/post-fix (1d8b526cfe600654c37c57e65b6cde84), confirming zero model/data drift from this prose-only fix.
- `run_meta.json` byte-identical pre/post-fix (diff exit 0).
- `summary.md` diff isolated to exactly one line (39c39), a redundant-literal removal with no information loss, verified by reading the surrounding context and confirming the dynamic rendering exists one clause earlier.
- Line counts 493=493.
- All three "PRIMARY (tradeable) beats B4: NONE" lines present and unchanged.
- Finding 1 rewrite: unambiguous present-tense current-state claim, history preserved in past tense, no remaining contradiction.
- Finding 2 section: all five sites, drift window, contamination mechanism, corrected magnitudes (cross-summed to 32,132 exactly, matches prior audit's independent numbers), R2 shift, no-verdict-change, and determinism check all present and numerically correct against the postrepair audit's own independently-derived ground truth.
- RUN_META.notes carries the DST-fix disclosure (dstfix_2026-07-31_fixes=[...]), consistent with `summary.md`.
- Spot-check (not full re-derivation, per instruction): `model_version` in the live `run_meta.json` (ensemble-cap1.5+calibsigma.2026-07-11) matches `api/modules/shared/locked_pace.py:22` exactly; `git_sha` (c762636) matches current repo HEAD. No drift.
- Prereg (`prereg/pattern_discovery_2026-07-26.md`) mtime unchanged: 2026-07-26 11:21:06.956665, identical to every prior audit round -- unedited across the entire five-repair campaign including this prose fix.
- File-scope fence: `extract_rules_2026-07-29.py`, `rules_out/`, `regime_*`, `combo_out/` all have mtimes on 2026-07-31 in the 12:10-15:04 range, consistent with the separately-noted concurrent builder activity in those paths (per the task brief), not this fix -- none of this study's files reference or depend on them, and none of the pattern_discovery outputs' numeric content changed in a way attributable to those files.

## What could NOT be checked

- Did not obtain a true source-control diff of the script (files remain untracked in git, same limitation as the postrepair round); relied on direct code reads at cited line numbers plus the full-file backup diff instead.
- Did not re-verify the underlying science (coverage repair correctness, DST-fix correctness at the 5 sites, determinism, elon_7day n=19 gating, THE WALL, trial_count, RNG split) -- per the task brief, these were fully cleared in the postrepair round and nothing about the models, gates, or data changed in this round; only the prose-fix mechanism and content were in scope this round.
- Did not independently re-verify whether the concurrent rules_out/regime_out/combo_out builder work is itself sound -- out of scope for this study's audit; noted only to confirm it did not touch pattern_discovery's files.

## What this PASS means and does not mean

It means: the `pattern_discovery_2026-07-26.py` deliverable is now internally consistent, self-documenting, and the disclosure prose is genuinely script-generated (survives a fresh run, confirmed dynamically, not statically) rather than artifact-only text that the next run would silently erase. Both postrepair findings -- the self-contradicting coverage paragraph and the undisclosed DST-fix campaign -- are resolved, and resolved accurately (the fix section's own numbers check out against the ground truth this audit independently derived last round). No numeric value, model, gate, or data row moved as part of this fix.

It does NOT mean the strategy works or that these markets have exploitable edge. The finding remains what it was in the postrepair round: no method beats the market on the full repaired sample (n=27/20/19 sealed auctions) -- market efficiency on the auctions and checkpoints this study actually tested, not proof the market is efficient everywhere, and not evidence against a tradeable edge existing in a form this diagnostic study wasn't built to find (the open question already on record: the 63-78% of Elon winners the market never priced).
